# 安全说明（Security）

## 报告漏洞

如发现安全漏洞，请**不要**公开披露，直接通过以下方式联系维护者：

- GitHub Issues（标注 `security` 标签）
- 或邮件至项目主页公布的联系邮箱

## 已知安全边界

本项目为**纯本地单机工具**，安全模型建立在以下假设之上：

- 服务仅监听 `127.0.0.1`，不对外网开放；请勿修改监听地址暴露到局域网/公网
- 上传文件与转写中间产物在处理完成后自动清理
- 模型文件从 Hugging Face 官方镜像（hf-mirror.com）下载，下载时尽力校验 SHA256
- 前端界面不依赖任何第三方 CDN，无外部脚本注入面

## 建议

- 保持 Node.js 与 whisper.cpp 引擎更新
- 不要在共享/不可信环境运行本工具（本机文件可被本机进程访问）
