# 更新日志（Changelog）

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v0.3.2] - 2026-09-01

### 修复
- **重复文字幻觉（说一遍变十遍）**：whisper 在长音频/模糊音频上会把同一句重复输出多次，且单条字幕内出现「屁股 - 屁股 - 屁股…」式同一词反复。新增双重后处理：
  - 相邻字幕段落文本完全相同 → 合并（保留首段时间起点 + 末段时间终点，重新编号）
  - 单段内整段同一词重复（≥2 字词 × 3 次以上，含 `*` 星号标记与 `�` 噪音字节）→ 收缩为单个词；笑声/语气词（哈哈哈/嗯嗯嗯）不受影响
  - txt / srt / vtt 三种输出同步处理

### 新增优化
- **图标 v3 重新设计**：亮青蓝渐变圆角方块（透明背景）+ 白色圆角波形条（音频波形概念）；修复 v1 图标 24bpp 白底问题（桌面白框根因）+ Windows 图标缓存残留（安装到桌面.bat 自动执行 ie4uinit.exe -show 刷新）
- **列表接口瘦身**：/api/tasks 不再返回 srt/vtt 大字段（标 `_trimmed`），前端展开任务时按需拉 /api/task/:id 详情——任务多时页面加载大幅提速
- **上传进度条**：上传改用 XHR，卡片实时显示「上传中」+ 百分比进度
- **文本编辑即存**：全文/SRT/VTT 文本可直接修改，改动即时写回（保存文件时自动用最新内容），字数栏显示「已编辑」
- **服务未运行提示**：轮询连续失败 3 次 → 提示「请双击启动拾音.bat」（不再静默卡死）
- 页面 theme-color 同步新图标主色

## [v0.3.1] - 2026-09-01

### 修复（重要）
- **启动拾音.bat / 安装到桌面.bat 双击无效果**：文件为 LF 行尾 + UTF-8 编码 + `chcp 65001`，cmd 在 65001 代码页下解析中文路径有缺陷（`if not exist "%~dp0启动拾音.bat"` 报 `'xist' is not recognized`，脚本提前失败）
  - 修复：改为 CRLF 行尾 + GBK 编码 + 去掉 `chcp 65001`（Windows 中文 bat 标准做法，GBK 原生环境稳定）
  - 实测：完整启动流程 / 已在运行快捷分支 / 桌面快捷方式创建（目标、工作目录、图标全对）均通过
- **新增 停止拾音.bat**：按端口 18900 精确查找 PID 停止（不用 `taskkill /IM node`，避免误杀其它 node 程序）；bat 内提示引用过该文件但此前缺失
- 顺带修复：随机密码生成器两个 bat 同样 LF 行尾问题（已转 CRLF，编码方案 GBK+chcp936 本就正确）

### 新增优化（v0.3.1 补充）
- **图标 v2**：重新设计——亮蓝渐变圆角方块（透明背景，无白底白框）+ 白色音符声波；16~256 七尺寸 ICO 保留 Alpha；PWA 缓存升 shiyin-v2 强制刷新
- **速度优化**：已是 16kHz 单声道 WAV 的上传文件跳过 ffmpeg 预处理直接识别（ffprobe 秒级探测）
- **上传校验**：前后端双层拦截非音视频文件（multer fileFilter → 400 友好提示；前端拖入即提示）
- **任务卡片**：显示「🧹 VAD」标识

### 其他
- 清理：项目内空目录（work/、scripts/）、缓存根目录 14 个自测残留文件（body.json、r1-r6.json 等）

## [v0.3.0] - 2026-09-01

### 新增
- **字幕跟读**：转写完成后保留原始媒体文件，结果页新增「📖 跟读」页签——内置播放器 + 字幕段落列表
  - 点击任意字幕行 → 从该处开始播放（精确跳转，误差 < 0.3s）
  - 播放时当前行自动高亮并滚动到可见区；空格键 播放/暂停
  - 支持音频与视频（mp4/mkv 等自动识别为视频播放器）
  - 媒体文件保留 2 小时（与任务记录同生命周期），到期自动清理
- **VAD 静音跳过**：新增静音检测模型（Silero VAD，ggml-silero-v6.2.0.bin，0.9MB）
  - 右侧面板一键下载；下载后可勾选「🧹 跳过静音段」再上传
  - 长音频/播客场景显著加速（只识别语音段），并减少环境声幻觉
  - 服务端：--vad + -vm 参数，VAD 模型未下载时给出明确提示
- 拖拽上传支持 VAD 选项；模型面板展示 VAD 模型状态

### 修复
- 媒体文件命名无扩展名（multer 临时文件名无扩展名导致浏览器无法播放）→ 改用原始文件名扩展名，MIME 正确（audio/wav 等）

### 技术细节
- 服务端：媒体目录 D:\\shiyin-cache\\work\\media\\<taskId>\\，express.static 自带 Range 支持（seek 可用，实测 206）
- 前端：SRT/VTT 统一解析器（parseCues），cueCache 缓存段落；播放器事件委托 + data-bound 幂等绑定
- 已知限制：词级时间戳（-owts 逐词高亮）未启用（段落级跳转已满足核心需求，保持转写速度）；VAD 对纯音乐类音频可能误判，建议语音类使用

## [v0.2.1] - 2026-09-01

### 新增
- 应用图标（app-icon.ico 多尺寸 + 512 PNG），PWA 化（manifest + service-worker，可安装到桌面、界面离线可用）
- 「安装到桌面.bat」：一键创建带图标的桌面快捷方式
- 启动脚本增强：端口占用检测、健康检查、自动打开浏览器、错误提示
- 输出文件夹记忆：选定一次后自动记住，下次保存免选（IndexedDB）
- README / LICENSE（MIT）/ SECURITY / CHANGELOG 文档补齐

### 优化
- **转写加速**：去掉 -pp 词级时间戳（-15%+），线程 4→8，批量并发 1→2
  - small 模型 60s 音频：单任务约 5s，双任务并行 10.2s（原来串行 20s）
  - turbo 模型 60s 音频：34.6s → 约 20s
- 模型选择持久化：重启服务后保持上次选择的模型

### 修复
- 中文文件名乱码（multer/busboy latin1 编码问题）—— 保存文件名现为正常中文
- 前端 result 字段映射错误（txt ↔ text）导致的文本框空白

## [v0.2.0] - 2026-09-01

### 新增
- 批量任务队列（并发排队）、任务卡片实时状态
- 模型管理：三模型注册表 + 页面切换 + 内置下载器（curl 断点续传 + SHA256 校验）
- txt / srt / vtt 三格式输出
- 反幻觉参数（-tpi 0 / -nth 0.6 / 温度 0）
- 无语音判定增强（兼容 (小声) / [音乐] 等括号标记）
- 结果本地缓存（localStorage，刷新不丢）

## [v0.1.0] - 2026-08-31

### 新增
- 首个可用版本：单文件转写（固定 small 模型）
- 拖拽上传、进度条、txt/srt 下载
- 中文路径兼容方案（D:\shiyin-cache ASCII 缓存目录）
- 繁转简输出
